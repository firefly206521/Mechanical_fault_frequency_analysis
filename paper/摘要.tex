\documentclass[withoutpreface,bwprint]{cumcmthesis}

\usepackage[framemethod=TikZ]{mdframed}
\usepackage{url}
\usepackage{subcaption}
\usepackage{geometry}
\usepackage{ulem}
\usepackage{setspace}
\usepackage{array}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{bm}
\usepackage{longtable}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{seqsplit}
\usepackage{float}
\usepackage{placeins}

\lstdefinestyle{pythonappendix}{
    language=Python,
    basicstyle=\ttfamily\scriptsize,
    keywordstyle=\color{blue!55!black},
    commentstyle=\color{green!35!black},
    stringstyle=\color{red!45!black},
    numbers=left,
    numberstyle=\tiny\color{gray},
    stepnumber=1,
    numbersep=7pt,
    frame=single,
    framesep=3pt,
    rulecolor=\color{black!25},
    breaklines=true,
    breakatwhitespace=false,
    showstringspaces=false,
    keepspaces=true,
    columns=fullflexible,
    tabsize=4,
    aboveskip=0.25em,
    belowskip=0.4em,
    lineskip=-1.1pt,
    captionpos=b
}
\lstset{style=pythonappendix}

\graphicspath{{./}{../q1_glrt_results/}{../q1_compare/}{../q2_results/}{../q3_results/}{../q3_results_complete/}{../q3_results/experiments/d/paper/}{../q4_results/paper/}}

\geometry{a4paper,left=25mm,right=25mm,top=25mm,bottom=25mm}
\title{基于广义似然比检验的机械设备微弱故障信号检测、恢复、分离与传感器布局优化}


\begin{document}
\xeCJKsetup{AutoFakeBold={2.5}}
% ===== 封面 =====
\newpage
\thispagestyle{empty}
\vspace*{0.5cm}

\begin{center}
    {\zihao{-2}\heiti\bfseries 2026年NUDT-CSU数学建模联赛}
\end{center}

\begin{center}
    \includegraphics[height=3cm]{图片1.png}\hspace{0.5cm}%
    \includegraphics[height=3cm]{图片2.png}
\end{center}

\vspace{1.5cm}

\makeatletter
\begin{center}
{\zihao{4}\bfseries\songti 题 目：\@title}
\end{center}
\makeatother

\vfill
\setcounter{page}{1}

\noindent{\zihao{-4}\bfseries\songti 摘 要：}

机械设备故障产生的周期信号能量微弱，常被电机运转和传动系统产生的强噪声完全淹没。传统谱峭度法在强噪声下峭度被压缩、易导致误报；最大相关峭度解卷积则依赖已知故障周期——而本题中故障频率恰好是待求的未知量。本文以\textbf{广义似然比检验（GLRT）}为核心框架，构建了从单频检测、波形恢复、多分量分离到传感器布局优化的渐进式方法链。

    \textbf{针对问题一：}对\texttt{data.xlsx}中"单源故障"工作表的数据进行线性去趋势后，采用归一化周期图扫描与纯噪声\textbf{Monte Carlo 门限校准}，在目标虚警率约$5\%$下检出故障频率$f_0 = 2.0000015$ Hz。相较于传统FFT峰值法，GLRT的核心优势在于提供了具有明确概率意义的统计判决门限，并通过连续频率精修突破FFT栅格分辨率限制。

    \textbf{针对问题二：}以问题一频率为初值建立\textbf{谐波回归}模型，恢复出$\hat{s}(t) = 0.03519 \sin(2\pi \times 2.0000016t + 1.5736)$，并采用局部渐近正态近似量化了参数的不确定性。\textbf{奇异谱分析（SSA）}对照波形的相关系数达$0.9956$；合成数据实验覆盖SNR $-20$至$-5$ dB，参数区间覆盖率均超过$93.5\%$。

    \textbf{针对问题三：}将单频GLRT扩展为基于\textbf{逐次干扰消除（SIC）}的顺序检测器，以联合谐波回归和\textbf{BIC准则}自动确定分量数量。在"多源故障"工作表中自动识别出4个周期分量，频率分别约为4、8、13、14 Hz。SNR $= -12$ dB时正确定阶率为$98.0\%$；条件化近频实验表明，$T = 400$ s条件下90\%经验辨识间隔约0.002--0.003 Hz，随振幅比增大而增宽。

    \textbf{针对问题四：}采用V1参数化结构模型，在6个区域生成120个无量纲候选点，经预筛选和组合搜索后以Monte Carlo验证排序。最优布局为齿轮箱右区点\texttt{v1\_p105}与齿轮箱左区点\texttt{v1\_p074}、\texttt{v1\_p062}，三点通过互补频响覆盖全部4个目标频率。SNR $= -12$ dB时平均分量检出率为$84.25\%$，误报率为$3.67\%$。该结论适用于本文给定的参数化几何与传播响应设置，不能直接解释为真实设备上的物理最优安装坐标。

    最后，本文方法链以GLRT为统一框架，具有虚警率可经验校准、分量数量可自动确定、辨识界限可条件化刻画的特点。四问从检测到部署共享核心统计框架，形成了完整的微弱故障信号处理方案。

\par\vskip1ex
\noindent{\zihao{-4}\bfseries\songti 关键词：}\textbf{广义似然比检验}，\textbf{微弱信号检测}，\textbf{谐波回归}，\textbf{多周期分量分离}，\textbf{传感器布局优化}

\end{document}
